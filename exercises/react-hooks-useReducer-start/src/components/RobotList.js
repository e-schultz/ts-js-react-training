import React from "react";

import RobotCard from "./RobotCard";

function RobotList({ robots, onRemove, onActivate, onDeactivate, ...props }) {
  return (<>Map over the robots to create a RobotCard</>)
}

export default RobotList;
