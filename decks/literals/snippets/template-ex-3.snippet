function greet(greeting, firstName, lastName, rate, hours) {
    return `${greeting} ${firstName} ${lastName}
You worked ${hours} at a rate of $${rate}/hour.
`
};

greet('Hello','Evan','Schultz',80,7.5)