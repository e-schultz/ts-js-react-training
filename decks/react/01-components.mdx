import { FullScreenCode, Split, SplitRight } from "mdx-deck/layouts";
import ReplIt from "../../components/replit";
import CodeSandbox from "../../components/codesandbox";
import Box from "../../components/Box";
import { Head, Appear, Image, withDeck } from 'mdx-deck'

## React Components

- Reusabble building blocks of the application
- Can take inputs from parent components, called props

---

## JSX

- "HTML" in JavaScript
- The standard way to write React components
- Can contain JavaScript which will be evaluated upon compilation
- Will be compiled to template literals and `React.createElement` calls

---

# React JSX Example 

<Box width={'75vw'}>

```javascript JSX Example

// This is identical...
const element = (
  <h1 className="greeting">
    Hello, world!
  </h1>
);

// To this
const sameElement = React.createElement(
  'h1',
  {className: 'greeting'},
  'Hello, world!'
);
```

</Box>
---

## What Happened?

- HelloWorld is the same in all cases - a function that will show text
- JSX-to-JavaScript compilation translates `<div>...</div>` into return value
---

# Component Types

---
export default Split 

# Stateless
# Stateful

---

export default Split 

# Stateless

- Super simple
- Just a function
- Give props
- Return dom

---
export default Split 

# Stateful

- Internal state
- Component Lifecycle Hooks
- Class that extends from React.Component

---

## Creating a Stateless Component

- Components are functions
- Should return a single JSX tag

---

## Stateless - Hello World Component

<Box width={'75vw'}>
  <CodeSandbox src="https://codesandbox.io/embed/1080oxnw33?eslint=1&fontsize=14&hidenavigation=1&module=%2Fsrc%2Findex.js&moduleview=1&highlights=4"/>
</Box>
---

## Stateless - Hello World - Children

<Box width={'75vw'}>
  <CodeSandbox src="https://codesandbox.io/embed/8l3wmjzk82?eslint=1&fontsize=14&hidenavigation=1&module=%2Fsrc%2Fhello-world.js"/>
</Box>



