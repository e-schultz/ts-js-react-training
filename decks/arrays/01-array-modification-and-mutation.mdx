import Code from "mdx-code";
import { Invert, FullScreenCode } from "mdx-deck/layouts";
import ReplIt from "../../components/ReplIt";

export default FullScreenCode;

### Array Modification & Mutation

<ReplIt src="https://repl.it/@e_schultz/Arrays-modification-and-mutation?lite=true" />

---

export default FullScreenCode;

### Arrays - Safely Operating

<ReplIt src="https://repl.it/@e_schultz/Arrays-Safely-Operating?lite=true" />

---

## What happened?

- Arrays cannot be copied through assignment
- `push` mutates `numbers`, which is the same as `numbers2`
- `Array.slice` with no indexes creates a copy of the array, so it is not modified
- Spreading the array into a new array creates a copy, so it is not modified

---

export default Code;

```javascript Accessing single array elements
const numbers = [1, 2, 3, 4, 5];
const index = 1;

console.log(
  numbers[index], // access with a variable
  numbers[index + 1] // access with an expression
);
```
