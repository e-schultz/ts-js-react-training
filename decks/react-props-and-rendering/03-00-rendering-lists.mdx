import { FullScreenCode, Invert } from "mdx-deck/layouts";
import { Head, Appear, Image, withDeck } from "mdx-deck";
import { CodeSurfer } from "mdx-deck-code-surfer";
import nightOwl from "prism-react-renderer/themes/nightOwl";
import ReplIt from "../../components/ReplIt";
import CodeSandbox from "../../components/CodeSandbox";
import Box from "../../components/Box";
const BRANCH_NAME = "master"; // tood: change before merge

export default Invert;

# Rendering Lists

---

## Rendering Lists

- Remember - you can pass any JavaScript value or object as a value to a Prop
- This includes Arrays
- You can use Array methods like `map`, `filter`, `reduce` to transform your data into components.

---

## Rendering Lists

- `map` is used to transform one object to another
- Can transform data into JSX
- Can also reduce a list of items into a Component
- Lists should specify a `key` attribute

---

### Rendering Lists - Keys

- Keys help React identify which items have changed, are added, or are removed.
- Keys should be given to the elements inside the array to give the elements a stable identity.
- If your record has an `id` - that is the most common attribute to use.

---

<CodeSurfer
  code={require("!raw-loader!./snippets/render-lists.snippet")}
  theme={nightOwl}
  lang="jsx"
  title="Rendering Lists"
  showNumbers
  steps={[
    {}, // This component renders from an array
    { lines: [1, 2, 3, 4, 5], notes: "People is an array of objects" },
    { lines: [7, 9], notes: "We map the array" },
    {
      lines: [10, 11, 12, 13],
      notes: "And pass object properties to another component"
    },
    {}
  ]}
/>

---

export default FullScreenCode;

### Rendering Lists - Example

<CodeSandbox
  src={`https://codesandbox.io/embed/github/e-schultz/ts-js-react-training/tree/${BRANCH_NAME}/examples/react-list-example?codemirror=1&fontsize=14&module=%2Fsrc%2FApp.js`}
/>

---

export default FullScreenCode;

### Exercise - RobotCard List - Start

<CodeSandbox
  src="https://codesandbox.io/embed/10x-list-robots-alternative-solution-2-4tvro?fontsize=14&hidenavigation=1&theme=dark"
  runOnClick={0}
/>

---

export default FullScreenCode;

### Exercise - RobotCard List - Solution

<CodeSandbox
  src="https://codesandbox.io/embed/10x-robot-list-v2-start-cgfv4?fontsize=14&hidenavigation=1&theme=dark"
  runOnClick={0}
/>
